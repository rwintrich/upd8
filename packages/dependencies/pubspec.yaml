name: dependencies
description: Dependencies package
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_modular: ^5.0.3
  result_dart: ^1.1.1
  flutter_bloc: ^8.1.5
  http: ^1.2.1

  login:
    path: ../login
  home:
    path: ../home
  profile:
    path: ../profile

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
